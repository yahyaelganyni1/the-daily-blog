<h1> hello from the user index</h1>

<ul>
<% @users.each do |user| %>
    <li> 
    <%= image_tag user.photo, height: 30 %>
    <%= link_to user.name, user_path(user.id) %>
    </li>
<%end%>
</ul>